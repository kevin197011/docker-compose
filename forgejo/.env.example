# Forgejo Runner 注册令牌
FORGEJO_RUNNER_REGISTRATION_TOKEN=your_generated_token_here

# Forgejo 实例地址配置
# 选择以下其中一种配置方式：

# 1. 内部访问（默认，适用于容器间通信）
FORGEJO_INSTANCE_URL=http://forgejo:3000

# 2. 外部IP访问（手动指定）
# FORGEJO_INSTANCE_URL=http://192.168.1.19:3000

# 3. 域名访问
# FORGEJO_INSTANCE_URL=http://forgejo.yourdomain.com

# Runner 配置
FORGEJO_RUNNER_NAME=forgejo-runner
FORGEJO_RUNNER_LABELS=ubuntu-latest:docker://node:20-bullseye,ubuntu-22.04:docker://node:20-bullseye,ubuntu-20.04:docker://node:20-bullseye

# PostgreSQL 数据库配置
FORGEJO_DB_NAME=forgejo
FORGEJO_DB_USER=forgejo
FORGEJO_DB_PASSWORD=change_me_to_a_strong_password
