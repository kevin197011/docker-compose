# Gitea Runner 注册令牌
GITEA_RUNNER_REGISTRATION_TOKEN=your_generated_token_here

# Gitea 实例地址配置
# 选择以下其中一种配置方式：

# 1. 内部访问（默认，适用于容器间通信）
GITEA_INSTANCE_URL=http://gitea:3000

# 2. 外部IP访问（手动指定）
# GITEA_INSTANCE_URL=http://192.168.1.19:3000

# 3. 域名访问
# GITEA_INSTANCE_URL=http://gitea.yourdomain.com

# Runner 配置
GITEA_RUNNER_NAME=gitea-runner
GITEA_RUNNER_LABELS=ubuntu-latest:docker://node:20-bullseye,ubuntu-22.04:docker://node:20-bullseye,ubuntu-20.04:docker://node:20-bullseye

# 启动脚本配置
# 是否自动使用外部IP（用于start.sh脚本）
USE_EXTERNAL_IP=false
GITEA_PORT=3000
