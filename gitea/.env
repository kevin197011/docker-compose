# Gitea with Act-Runner Environment Configuration
# Copy this file to .env and modify the values as needed

# Global Runner Registration Token (recommended - 自动生成和管理)
# This token will be automatically set for the Gitea instance
# Generate a random token: openssl rand -hex 24
GITEA_RUNNER_REGISTRATION_TOKEN=ce1858ef4cb9584093572597c1092b1364e8432be2554fed

# Alternative: Manual Registration Token (legacy method)
# Only use this if you don't want to use the global token
# ACT_RUNNER_TOKEN=your_manual_token_here

# Act-Runner Name (optional, will use hostname if not set)
ACT_RUNNER_NAME=gitea-runner

# Act-Runner Labels (optional, comma-separated)
# Default: ubuntu-latest:docker://node:20-bullseye,ubuntu-22.04:docker://node:20-bullseye,ubuntu-20.04:docker://node:20-bullseye
ACT_RUNNER_LABELS=ubuntu-latest:docker://node:20-bullseye,ubuntu-22.04:docker://node:20-bullseye,ubuntu-20.04:docker://node:20-bullseye

# Additional labels you can use:
# ACT_RUNNER_LABELS=ubuntu-latest:docker://node:20-bullseye,linux:host,self-hosted:host
