# Gitea 配置示例
# 复制此文件为 .env 并修改相应的值

# 生成 runner 注册令牌
GITEA_RUNNER_REGISTRATION_TOKEN=your_generated_token_here

# 外部访问配置 - 解决 CI/CD 中的主机名解析问题
# 重要：将下面的 IP 地址替换为你的实际 IP 地址或域名

# 使用 IP 地址的配置：
GITEA_ROOT_URL=http://192.168.1.19:3000/
GITEA_DOMAIN=192.168.1.19

# 使用域名的配置（如果你有域名）：
# GITEA_ROOT_URL=http://gitea.yourdomain.com/
# GITEA_DOMAIN=gitea.yourdomain.com

# 使用 HTTPS 的配置（如果你有 SSL 证书）：
# GITEA_ROOT_URL=https://gitea.yourdomain.com/
# GITEA_DOMAIN=gitea.yourdomain.com

# Runner 配置
ACT_RUNNER_NAME=gitea-runner
ACT_RUNNER_LABELS=ubuntu-latest:docker://node:20-bullseye,ubuntu-22.04:docker://node:20-bullseye,ubuntu-20.04:docker://node:20-bullseye

# 数据库配置（如果需要自定义）
# DB_TYPE=postgres
# DB_HOST=postgres
# DB_NAME=gitea
# DB_USER=gitea
# DB_PASSWD=gitea@passwd
